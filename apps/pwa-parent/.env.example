# PWA Parent (independent service)
VITE_PORT=13510
# Dev server proxies /api and /storage to this target (the Sinapsis API).
VITE_API_PROXY_TARGET=http://localhost:14000

# Axios base path. Rewired to the Sinapsis API surface (/api/*) via lib/data.ts.
VITE_API_URL=/api
